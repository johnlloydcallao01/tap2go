{
  "name": "firebase-config",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "npx tsc",
    "dev": "npx tsc --watch",
    "lint": "eslint src/",
    "type-check": "npx tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "firebase": "^11.8.1",
    "firebase-admin": "^13.4.0",
    "shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
