'use client';

// /restaurants route - renders the main homepage with stores view
// This completely replaces the old standalone restaurants page
import Home from '../page';

export default function RestaurantsPage() {
  return <Home />;
}
