# Force pnpm usage in Vercel
package-manager=pnpm

# Enable hoisting for monorepo compatibility
hoist-pattern[]=*

# Enable auto-install peers to match root configuration
auto-install-peers=true

# Use workspace protocol
enable-pre-post-scripts=true
