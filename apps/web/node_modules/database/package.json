{
  "name": "database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "shared-types": "workspace:*",
    "firebase-config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
